#!/usr/bin/env bash
set -euo pipefail

dir="$(cd "$(dirname "$0")" && pwd)"
app_dir="/tmp/onix-tests/redmine"

if [ ! -d "$app_dir" ]; then
  echo "Cloning redmine to $app_dir..."
  git clone --depth 1 https://github.com/redmine/redmine.git "$app_dir"
fi

cd "$app_dir"
rm -rf tmp/cache/bootsnap 2>/dev/null || true

exec nix-shell "$dir/devshell.nix" --run "
  bin/rails --help >/dev/null
  echo 'redmine: rails boot OK'
"
