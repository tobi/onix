#!/usr/bin/env bash
set -euo pipefail

# Run liquid's unit tests inside its nix devshell.
#
# Usage:
#   tests/liquid/run-tests

dir="$(cd "$(dirname "$0")" && pwd)"
app_dir="$HOME/src/ruby-tests/liquid"

cd "$app_dir"

exec nix-shell "$dir/devshell.nix" --run "
  bundle exec rake test
"
